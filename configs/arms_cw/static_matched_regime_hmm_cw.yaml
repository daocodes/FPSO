# TURNOVER-MATCHED CONTROL for `regime_hmm_cw`, under FPSO-CW.
#
#   target turnover   : 0.1928
#   achieved turnover : 0.1912
#   solved lambda_t   : 0.02344  (baseline 0.01)
#
# A control, not a strategy: matched on turnover, never on performance.
extends: ../base.yaml
name: static_matched_regime_hmm_cw
optimizer: fpso_cw
base_params:
  lambda_t: 0.02344
regime:
  detector: none
  overrides: {}
